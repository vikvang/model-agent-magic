# API Keys
# -------------
# Your API key for Perplexity AI (or compatible OpenAI-style API)
OPENAI_API_KEY=your_api_key_here
# Base URL for Perplexity API (or OpenAI-compatible endpoint)
PERPLEXITY_BASE_URL=https://api.perplexity.ai

# Environment Settings
# -------------
# Environment type (development, production)
ENVIRONMENT=development
# Frontend URL for CORS
FRONTEND_URL=chrome-extension://*/

# Server Settings
# -------------
# Host address (0.0.0.0 allows external connections)
HOST=0.0.0.0
# Port to run the server on
PORT=8000

# Database & Auth Settings
# -------------
# Supabase URL (required for production)
SUPABASE_URL=your_supabase_url_here
# Supabase API key (required for production)
SUPABASE_KEY=your_supabase_key_here
# JWT secret for authentication tokens (change for production!)
JWT_SECRET=your-secret-key-for-development-only
# JWT token expiration in minutes
JWT_EXPIRATION_MINUTES=60

# Logging Settings
# -------------
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
# Environment (development, production)
ENVIRONMENT=development
# Log file location (used in production)
# LOG_FILE=logs/gregify.log

# Model Settings
# -------------
# Default model to use
DEFAULT_MODEL=sonar
# Default temperature for generations
DEFAULT_TEMPERATURE=0.7
# Default maximum tokens for generations
DEFAULT_MAX_TOKENS=1500
# Random seed for reproducibility
SEED=42

# Note: Rename this file to .env and add your actual values to use the application
# The API key is used for both the Multi-Agent System and the Normal mode 